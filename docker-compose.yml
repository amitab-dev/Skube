services:
  database:
    image: postgres:17
    container_name: skube_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: skube
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data: